import { Colors, Box } from "@/constants";

export const Divider = () => {
  return (
    <Box
      bg="inactiveTabBarIcon"
      style={{
        height: 1,
        width: "100%",
      }}
    />
  );
};
